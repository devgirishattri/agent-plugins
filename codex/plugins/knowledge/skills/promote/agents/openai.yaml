# Per-skill Codex invocation policy: explicit invocation only.
# Twin of Claude's disable-model-invocation: this skill is user-run only and
# must never be implicitly invoked by the model.
policy:
  allow_implicit_invocation: false
